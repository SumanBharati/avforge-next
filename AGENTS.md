# AVForge — Project Notes for Codex

## Competitive Reference
- AV Buddy teardown: docs/competitive/av-buddy/video-teardown.md
  (screenshot index inside maps IMG files to screens)
- Rule: never hand-type derived data (aspect ratios, Mpx, bandwidth) —
  always compute from base values. See teardown §4 for why.
